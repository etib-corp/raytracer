/*
** EPITECH PROJECT, 2024
** B-OOP-400-RUN-4-1-raytracer-julien.ferdinand
** File description:
** Rotation
*/

#ifndef ROTATION_HPP_
#define ROTATION_HPP_

#include <cstdint>

/**
 * @brief Represents a rotation in 3D space.
 */
struct Rotation {
    int32_t x; /**< The rotation around the x-axis. */
    int32_t y; /**< The rotation around the y-axis. */
    int32_t z; /**< The rotation around the z-axis. */
};

#endif /* !ROTATION_HPP_ */
